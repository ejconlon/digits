machine:
  timezone:
    America/Los_Angeles # Set the timezone

  python:
    version:
      2.7.12

dependencies:
  override:
    - make deps-linux
  cache_directories:
    - data

test:
  override:
    - make all
