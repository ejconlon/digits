machine:
  timezone:
    America/Los_Angeles # Set the timezone

dependencies:
  override:
    - rm -rf virtualenvs venv .pyenv
    - make deps-linux
    - make fetch

  cache_directories:
    - data

test:
  override:
    - make all
