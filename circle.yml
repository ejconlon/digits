machine:
  timezone:
    America/Los_Angeles
  environment:
    PATH: /usr/bin:$PATH

dependencies:
  override:
    - make deps-linux
    - make fetch
  cache_directories:
    - data

test:
  override:
    - make all
