machine:
  timezone:
    America/Los_Angeles # Set the timezone

  python:
    version:
      2.7.12

dependencies:
  override:
    - apt-get install graphviz texlive-full inkscape python-numpy
    - pip install -r requirements.txt

test:
  override:
    make clean fetch test-verbose
