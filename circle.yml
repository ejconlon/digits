machine:
  timezone:
    America/Los_Angeles # Set the timezone

  python:
    version:
      2.7.12

dependencies:
  override:
    - make deps-linux
    - make fetch

  cache_directories:
    - data

test:
  override:
    - make all
